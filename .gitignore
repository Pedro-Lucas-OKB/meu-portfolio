# Dados pessoais (protótipo + PDF do currículo) — nunca commitar
local_files/

# Secrets e ambiente
.env
.env.local

# Dependências e build
node_modules/
dist/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
# .terraform.lock.hcl NÃO entra aqui — esse arquivo deve ser commitado
# (trava as versões exatas dos providers, como um package-lock.json)

# Editor / SO
.DS_Store
.vscode/*
.idea/

# .NET build artifacts
bin/
obj/
*.user
